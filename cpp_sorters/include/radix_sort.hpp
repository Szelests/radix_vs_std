#pragma once
#include <vector>

/**
 * @brief Ordena um vetor de números inteiros não-negativos utilizando o algoritmo Radix Sort (Base 256).
 * @param arr Referência para o vetor de inteiros que será ordenado in-place.
 */
void radixSort(std::vector<int>& arr);